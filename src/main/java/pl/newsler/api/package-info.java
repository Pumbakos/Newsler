/**
 * Exposes public APIs, interfaces only, no implementation class should be created in this and nested packages
 * except for API-specific exceptions.
 * All requests are secured not only by HTTPS but also via AES.
 *
 * @author Pumbakos
 * @since 0.0.1
 */
package pl.newsler.api;